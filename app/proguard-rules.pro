# The app has no reflection, no serialization and no third-party SDKs, so the
# default optimised rules are enough. Keep line numbers for readable stack traces.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
